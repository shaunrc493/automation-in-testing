describe('Performance audit', { tags: ['@performance', '@non-functional'] }, () => {
    it('should inject axe and check for performance violations', () => {
        cy.visit('/');
        cy.viewport(1920, 1080);

        
        
    });
});
